# تقرير مراجعة وإصلاح مشروع Monta Foam

**تاريخ المراجعة:** 29 أغسطس 2026
**النطاق:** كود الواجهة الأمامية والخلفية، قاعدة البيانات، التكامل بينهما، الأمان، البناء، الاعتماديات، وتجربة الواجهات الأساسية.
**النتيجة:** تم إصلاح جميع العيوب المكتشفة داخل نطاق المنصة الأساسية (Phase 1)، وأصبح الـFrontend والـBackend قابلين للبناء والتشغيل والتكامل دون أخطاء معروفة في مسارات الاختبار المنفذة.

## ملخص العيوب التي تم إصلاحها

| # | الخطورة | العيب المكتشف | الإصلاح والتحقق |
|---:|:---:|---|---|
| 1 | حرجة | إعداد TypeScript في الـBackend جمع بين `CommonJS` و`Bundler`، لذلك كان البناء يفشل. | تغيير module resolution إلى Node؛ `npm run build` ينجح. |
| 2 | حرجة | Axios interceptor كان يستدعي `/auth/refresh` من نفس العميل بعد فشل refresh، مسببًا حلقة لا نهائية وعشرات الطلبات ثم `429`. | عميل refresh مستقل، منع retry لمسارات auth، وsingle-flight promise؛ تم اختبار refresh rotation ومنع replay. |
| 3 | حرجة | `COOKIE_SECURE=false` كانت تتحول إلى `true` بسبب `z.coerce.boolean()`، وبالتالي المتصفح المحلي لا يرسل cookies. | parser صريح لقيم true/false؛ تم التحقق من جلسة login/refresh محليًا. |
| 4 | عالية | Cookie domain كان `localhost` صراحة، وهو غير متوافق بصورة ثابتة بين المتصفحات. | حذف Domain محليًا لتصبح cookie من نوع host-only، مع إبقاء إعداد الإنتاج قابلًا للضبط. |
| 5 | عالية | الواجهة الاحتياطية ترسل service slug بينما الـBackend يقبل UUID فقط؛ إنشاء الطلب يفشل برسالة `Invalid uuid`. | قبول UUID أو slug، تحويله إلى service ID، والتحقق من أن الخدمة موجودة ونشطة. تم إنشاء طلب فعلي بالـslug بنجاح. |
| 6 | عالية | طلب الخدمة العام لم يكن يقرأ جلسة المستخدم اختياريًا، لذلك الطلب المسجل لا يرتبط بصاحبه. | إضافة optional authentication؛ اختبار التكامل أكد `userLinked=ok`. |
| 7 | عالية | استجابة تأكيد البريد لا ترجع user بينما الواجهة تعتمد عليه لتكوين الجلسة. | توحيد contract وإرجاع بيانات المستخدم المتوقعة. |
| 8 | عالية | فتح تبويب جديد يفقد حالة الدخول رغم وجود refresh cookie صالحة. | استعادة آمنة للجلسة باستخدام session marker وrefresh cookie دون تخزين JWT في localStorage. |
| 9 | عالية | المستند يذكر real-time notifications لكن الواجهة لم يكن بها Socket.io client. | hook موحد للـSocket، fallback polling، تحديث cache، وعدّاد unread في لوحة التحكم. |
| 10 | عالية | Google OAuth موجود في الـBackend فقط ولا يمكن استعماله من صفحات الدخول والتسجيل. | إضافة Google Identity button وربطه بالـAPI، مع رفض حساب Google ذي البريد غير المؤكد. |
| 11 | عالية | Swagger يعمل شكليًا لكنه يعرض صفر مسارات. | بناء OpenAPI spec كامل وإضافة 27 route وschemas، مع endpoint خام `/api-docs.json`. |
| 12 | عالية | البريد موصوف بأنه Resend مع SMTP fallback، لكن فشل Resend لا ينتقل فعليًا إلى SMTP ولا يتم فحص `result.error`. | فحص نتيجة Resend، تسجيل الفشل، ومحاولة SMTP fallback الحقيقية. |
| 13 | متوسطة | تحديث الخدمات بلا validation كافٍ، والـslug العربي قد ينتج قيمة فارغة. | partial validation وتوليد Unicode slug يدعم العربية. |
| 14 | عالية | مستخدم بدور ADMIN يستطيع تغيير أدوار إداريين آخرين. | قصر تغيير الأدوار على MASTER_ADMIN فقط. |
| 15 | متوسطة | نافذة طلب الخدمة تحتفظ بحالة النجاح والبيانات عند إعادة فتحها، ورسالة الخطأ لا تعكس خطأ الـAPI. | reset كامل عند الفتح، رسالة API حقيقية، وتحسين خصائص dialog للوصول. |
| 16 | عالية | CORS وحده لا يمنع كل الطلبات المعدّلة القادمة من Origin خارجي عند استخدام cookies. | إضافة trusted-origin guard لطلبات POST/PATCH/PUT/DELETE، مع SameSite قابل للضبط؛ الطلب الخبيث الاختباري رُفض `403`. |
| 17 | متوسطة | الواجهة تحتوي على أرقام خبرة، شهادات، فريق وعناوين افتراضية قد تُعرض كحقائق عن الشركة. | إزالة المحتوى المصطنع، وإظهار معلومات مؤكدة فقط أو إخفاء الحقول غير المضبوطة. |
| 18 | متوسطة | بيانات الخدمات seed كانت إنجليزية ولا تطابق الواجهة العربية. | تحديث seed وتشغيله؛ قاعدة البيانات الآن تحتوي 5 خدمات عربية. |
| 19 | متوسطة | الهوية البصرية والخطوط لا تطابق التوثيق، والصور المستخدمة ليست صور المشروع. | تطبيق ألوان Monta Foam وخط Cairo واستخدام الصورة المتاحة من الصفحة الرسمية، مع عدم اختلاق باقي الصور. |
| 20 | عالية | نسخ Next.js/PostCSS/Nodemailer كانت تحتوي على تنبيهات أمنية. | ترقية الإصدارات المتوافقة؛ `npm audit` = صفر في المشروعين. |
| 21 | متوسطة | أمر lint في الـBackend مكسور لأن ESLint غير مثبت ولا توجد flat config. | إضافة ESLint/TypeScript ESLint وإعداد صحيح؛ lint يمر في المشروعين. |
| 22 | متوسطة | قوالب البريد وعنوان API ما زالت تحمل اسم المشروع القديم، والقيم الديناميكية داخل HTML غير escaped. | تعريب القوالب، تطبيق هوية Monta Foam، وHTML escaping للاسم والخدمة والكود. |
| 23 | منخفضة | أثر إنشاء طلب الخدمة لا يحتوي request ID في metadata، مما يصعب التتبع. | إضافة request ID لسجل المراجعة. |

## ما تم تنفيذه أو استكماله

- موقع عربي RTL متجاوب: الرئيسية، الخدمات، من نحن، تواصل معنا، وصفحات المصادقة.
- تسجيل/دخول/OTP/نسيت كلمة المرور/Google Sign-In، مع access وrefresh rotation آمنين.
- لوحة تحكم: الإحصائيات، الطلبات وتغيير حالتها، المستخدمون والأدوار، والإشعارات اللحظية.
- تقديم طلب للزائر أو المستخدم، ربطه بالخدمة، إنشاء رابط WhatsApp، وإشعار الإدارة.
- RBAC لثلاثة أدوار، audit logging، Helmet، CORS، HPP، sanitization، rate limiting، وorigin protection.
- توثيق Swagger قابل للتجربة، seed للخدمات والمدير الرئيسي، وقاعدة Prisma migration محدثة.
- قوالب بريد HTML عربية تحمل هوية Monta Foam، مع Resend وSMTP fallback.

## نتائج الاختبارات الفعلية

| الاختبار | النتيجة |
|---|---|
| Frontend lint | ناجح |
| Frontend production build | ناجح — 16 صفحة static و13 route rows |
| Backend lint | ناجح |
| Backend TypeScript build | ناجح |
| `npm audit` Frontend | 0 vulnerabilities |
| `npm audit` Backend | 0 vulnerabilities |
| Prisma migration status | Up to date |
| HTTP للصفحات العامة وصفحات المصادقة ولوحة التحكم | `200` |
| Health endpoint | `success=true` |
| Swagger | 27 paths |
| CORS preflight | `204` مع Origin الصحيح |
| Untrusted origin mutation | `403` |
| Login + refresh rotation | ناجح |
| إعادة استخدام refresh token القديم | `401` كما هو مطلوب |
| إنشاء طلب باستخدام service slug | ناجح |
| ربط الطلب بالمستخدم | ناجح |
| تحديث حالة الطلب | `IN_PROGRESS` |
| وصول notification للإدارة | ناجح |
| تنظيف بيانات اختبار التكامل | تم حذف الطلب والإشعار وسجل المراجعة الاختباري |
| فحص مرئي Desktop/Mobile/RTL | ناجح للصفحة الرئيسية والخدمات والدخول |

## أشياء تحتاج بيانات تشغيل حقيقية قبل النشر العام

هذه ليست bugs برمجية، ولا يصح اختلاق قيم لها:

1. ربط domain الإنتاج وضبط `CLIENT_URL` و`API_URL` و`NEXT_PUBLIC_API_URL` وخصائص cookies للإنتاج.
2. توثيق domain مرسل في Resend أو إدخال SMTP حقيقي. الإعداد الاحتياطي الحالي في `.env` مثال فقط، لذلك لم يتم الادعاء بوصول بريد خارجي فعلي.
3. إضافة Google production origins/callback URLs في حساب Google Cloud.
4. تزويد عنوان الشركة والبريد التجاري وساعات العمل إن كان مطلوبًا إظهارها.
5. تزويد باقي صور المشروعات الأصلية؛ المتاح المؤكد حاليًا صورة واحدة فقط، ولم تُستخدم صور stock على أنها أعمال الشركة.

## حدود الـRoadmap

المستند يصف **Phase 1** بأنها Core Platform وComplete، وهي نطاق التسليم الذي تم إصلاحه والتحقق منه. أما **Phase 2** فهي موسومة `Next` و**Phase 3** موسومة `Future` وليستا عيوبًا في النسخة الأساسية. تم إنجاز بند قوالب البريد الغنية من Phase 2 أثناء الإصلاح، بينما تظل البنود التالية تطويرًا لاحقًا حسب المستند: رفع الصور/CMS، إدارة الخدمات من الواجهة، CSV/Excel، العربية والإنجليزية، الخرائط، تغيير كلمة المرور والملف الشخصي، ثم خصائص النمو مثل عروض الأسعار والفواتير وPWA وSMS.

## روابط التشغيل المحلية

- الموقع: <http://localhost:3000>
- الـAPI: <http://localhost:5000>
- Swagger: <http://localhost:5000/api-docs>
- Health: <http://localhost:5000/api/v1/health>
